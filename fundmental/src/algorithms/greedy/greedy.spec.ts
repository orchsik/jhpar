import { solution } from './11000';

describe('', () => {
  it('test1', () => {
    var inputs = [
      [1, 3],
      [2, 4],
      [3, 5],
    ];
    var answer = solution(inputs);
    // expect(answer).toEqual([1, 4, 0]);
  });
});
