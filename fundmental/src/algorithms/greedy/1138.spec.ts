import { solution } from './1138';

describe('', () => {
  it('', () => {
    const lookList = [2, 1, 1, 0];
    const R = solution(lookList.length, lookList);
    expect(R).toEqual([4, 2, 1, 3]);
  });
});
