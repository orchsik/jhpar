import { solution } from './4796';

describe('', () => {
  it('', () => {
    const testCaseList = ['5 8 20', '5 8 17', '0 0 0'];
    const R = solution(testCaseList);
    expect(R).toEqual([14, 11]);
  });
});
