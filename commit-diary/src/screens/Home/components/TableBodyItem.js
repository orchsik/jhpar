import S from "styled-components";

import {} from "../../../styles/colors";

const TableBodyItem = S.ul({
  display: "flex",
  margin: 0,
  padding: 0,
  height: "6.5vh",
  width: "100%",
  justifyContent: "center",
  alignItems: "center",
  // backgroundColor: OpenColor.lime[i % 7]
});

export default TableBodyItem;
