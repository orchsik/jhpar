{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "label": "Run nodemon",
      "command": "nodemon",
      "args": ["${fileDirname}/${fileBasenameNoExtension}.js"], // Window
      // "args": ["${fileDirname}\\${fileBasenameNoExtension}.js"],
      "group": {
        "kind": "test",
        "isDefault": true
      }
    },
    {
      "type": "shell",
      "label": "Run node",
      "command": "node",
      "args": ["${fileDirname}/${fileBasenameNoExtension}.js"], // Window
      // "args": ["${fileDirname}\\${fileBasenameNoExtension}.js"],
      "group": {
        "kind": "test",
        "isDefault": true
      }
    },
    {
      "type": "shell",
      "label": "Run ts-node",
      "command": "ts-node",
      "args": ["${fileDirname}/${fileBasenameNoExtension}.ts"], // Window
      // "args": ["${fileDirname}\\${fileBasenameNoExtension}.ts"],
      "group": {
        "kind": "test",
        "isDefault": true
      }
    }
  ]
}
